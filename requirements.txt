# ==========================================
# PACE: Pipelined Actor-Critic Ensemble
# Core Requirements
# ==========================================

# --- Core Machine Learning ---
# Note: For your RTX 4060, you must install PyTorch with CUDA 12.1+ BEFORE running this file using:
# pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu121
torch>=2.2.0
transformers>=4.38.0
accelerate>=0.27.0
safetensors>=0.4.2

# --- API & Backend (FastAPI) ---
fastapi>=0.110.0
uvicorn[standard]>=0.28.0
pydantic>=2.6.0
pydantic-settings>=2.2.0

# --- Rust/Python Integration ---
# Required to build and bind your Rust tokenizers via PyO3
maturin>=1.5.0

# --- Data Handling & Parsing ---
datasets>=2.18.0
polars>=0.20.0
pyyaml>=6.0.1
aiofiles>=23.2.1

# --- Evaluation & Metrics (For the Critics) ---
evaluate>=0.4.1
rouge_score>=0.1.2
bert_score>=0.3.13

# --- Version Control ---
# Allows tracking of the 6 model variants without bloating Git
dvc>=3.48.0
